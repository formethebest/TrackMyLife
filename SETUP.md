# 🚀 TrackMyLife - Инструкция по запуску

## Шаг 1: Установка зависимостей

Откройте терминал в папке проекта и выполните:

```bash
cd C:\Users\mihee\.gemini\antigravity\scratch\trackmylife
npm install
```

Это установит все необходимые пакеты (Next.js, React, Tailwind CSS, shadcn/ui, Recharts и др.)

## Шаг 2: Запуск dev-сервера

После установки зависимостей запустите:

```bash
npm run dev
```

## Шаг 3: Откройте в браузере

Откройте [http://localhost:3000](http://localhost:3000)

Вы должны увидеть главную страницу TrackMyLife! 🎉

## Что дальше?

### Тестирование приложения:
1. Нажмите "Добавить трекер"
2. Создайте трекер (например, "Instagram", количественный, минут, красный цвет)
3. Введите значение на главной странице
4. Нажмите на иконку графика для просмотра аналитики

### Деплой на Vercel:

1. **Создайте репозиторий на GitHub:**
```bash
git init
git add .
git commit -m "Initial commit: TrackMyLife MVP"
git branch -M main
git remote add origin https://github.com/formethebest/TrackMyLife.git
git push -u origin main
```

2. **Подключите к Vercel:**
   - Зайдите на [vercel.com](https://vercel.com)
   - Нажмите "New Project"
   - Импортируйте репозиторий `formethebest/TrackMyLife`
   - Нажмите "Deploy"
   - Через 2-3 минуты получите ссылку на приложение!

## Возможные проблемы:

### Ошибка "Module not found"
```bash
npm install
```

### Ошибка с портом 3000
```bash
npm run dev -- -p 3001
```

### Ошибка TypeScript
```bash
npm run build
```

## Структура проекта:

```
trackmylife/
├── app/                    # Страницы Next.js
│   ├── page.tsx           # Главная страница
│   ├── analytics/[id]/    # Страница аналитики
│   └── layout.tsx         # Root layout
├── components/            # React компоненты
│   ├── ui/               # shadcn/ui компоненты
│   ├── tracker-card.tsx  # Карточка трекера
│   ├── tracker-form.tsx  # Форма создания/редактирования
│   └── theme-toggle.tsx  # Переключатель темы
├── lib/                  # Утилиты
│   ├── storage.ts        # Работа с localStorage
│   ├── date-utils.ts     # Работа с датами
│   └── utils.ts          # Общие утилиты
├── types/                # TypeScript типы
│   └── index.ts
└── package.json          # Зависимости
```

## Готово! 🎉

Теперь у вас есть полностью рабочий MVP трекера привычек!
